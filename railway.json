{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && npm run build",
    "nixpacksPlan": {
      "providers": ["node"]
    }
  },
  "deploy": {
    "startCommand": "npm start"
  }
}